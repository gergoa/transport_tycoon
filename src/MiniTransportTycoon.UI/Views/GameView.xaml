<UserControl x:Class="MiniTransportTycoon.UI.Views.GameView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MiniTransportTycoon.UI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="750" d:DesignWidth="750">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Menu Grid.Row="0">
            <MenuItem Header="Tick" Command="{Binding TickCommand}"/>
        </Menu>
        <ItemsControl Grid.Row="1" ItemsSource="{Binding Fields}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="{Binding SizeN}" Rows="{Binding SizeM}" Height="700" Width="700"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemContainerStyle>
                <Style>
                    <Setter Property="Control.Padding" Value="0"/>
                    <Setter Property="Control.Margin" Value="0"/>
                </Style>
            </ItemsControl.ItemContainerStyle>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Rectangle StrokeThickness="0.5">
                        <Rectangle.Style>
                            <Style TargetType="Rectangle">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding}" Value="EMPTY">
                                        <Setter Property="Fill" Value="LightGreen"/>
                                        <Setter Property="Stroke" Value="DarkGray"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding}" Value="FOREST">
                                        <Setter Property="Fill" Value="Green"/>
                                        <Setter Property="Stroke" Value="LightGray"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding}" Value="WATER">
                                        <Setter Property="Fill" Value="Blue"/>
                                        <Setter Property="Stroke" Value="LightGray"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding}" Value="ROAD">
                                        <Setter Property="Fill" Value="#464646"/>
                                        <Setter Property="Stroke" Value="White"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding}" Value="BRIDGE">
                                        <Setter Property="Fill" Value="OrangeRed"/>
                                        <Setter Property="Stroke" Value="DarkGray"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding}" Value="FACILITY">
                                        <Setter Property="Fill" Value="DarkViolet"/>
                                        <Setter Property="Stroke" Value="LightGray"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Rectangle.Style>
                    </Rectangle>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <StatusBar Grid.Row="2">
            <StatusBarItem>
                <TextBlock Text="Money: "/>
            </StatusBarItem>
            <StatusBarItem>
                <TextBlock Text="{Binding Money}"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</UserControl>
